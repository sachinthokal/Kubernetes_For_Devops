apiVersion: v1
kind: Pod
metadata:
  name: env-config-pod
spec:
  containers:
    - name: busybox
      image: busybox:latest
      command: ["sh", "-c", "env && sleep 3600"]
      envFrom:
        - configMapRef:
            name: app-config
